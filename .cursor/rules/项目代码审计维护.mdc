---
description: 项目代码审计文档的维护规则——每次代码迭代后必须同步更新 项目代码审计.md
alwaysApply: true
---

# 项目代码审计文档维护

本项目所有 agent 在做任何代码改动时，必须遵守以下规则：

## 文档定位

`项目代码审计.md`（位于项目根目录）是本项目的**权威审计与架构说明文档**。它的作用是让任何后续接手的 agent / 人在 5 分钟内掌握代码框架、已知约束、未修风险与改进路线，避免每次都重新审计全代码。

`REFACTOR_TODO.md` 是早期技术债清单，**已让位给 `项目代码审计.md`**。两者内容冲突时以 `项目代码审计.md` 为准。

## 必须执行的维护动作

### 1. 会话开始时

- **先读 `项目代码审计.md`**：在做任何代码改动前，必须先读完该文档，理解当前架构、约束、未修风险。
- 不要重复审计已经写明的风险/限制——先看文档。

### 2. 完成代码迭代后

每次完成代码改动（不论是修 bug、加功能还是重构），**必须同步更新 `项目代码审计.md`**，至少包括：

1. **更新顶部「最后更新时间」和「最近一次更新原因」**
2. **更新对应章节**：
   - 修改了架构/数据流 → 更新「一、整体架构与数据流」
   - 新增/移除/修改了硬规则约束 → 更新「二、关键约束与保护机制」表
   - 修复了某个风险 → 把「三、客观限制与潜在风险」中对应条目状态从 🔴/🟡 改为 🟢，并在条目末尾追加 `(已修于 YYYY-MM-DD：简要说明)`
   - 引入了新风险 → 在「三、客观限制与潜在风险」中新增条目，按严重程度归类
   - 完成了某项改进 → 把「四、改进与迭代优化空间」表中对应行的「状态」从「待办」改为「已完成」+ 简要说明
3. **追加变更日志**：在「五、变更日志」表格末尾追加一行：
   ```
   | YYYY-MM-DD | 改了什么（一句话） | 是 |
   ```

### 3. 状态符号约定

文档中风险条目和改进项的状态符号：

- 🔴 = 严重未修
- 🟡 = 中等 / 已知
- 🟢 = 已修 / 轻度
- ⚪ = 待确认
- ✅ = 改进项已完成

## 何时可以例外

仅以下情况可以不更新文档：

- **纯文字/样式/注释调整**（无逻辑变化、无新风险引入）
- **临时实验性改动**（明确告诉用户不会合入）

如果不确定改动的范围，**默认要更新文档**。

## 不要做的事

- 不要把 `项目代码审计.md` 改成纯 changelog——它必须始终保持"现状快照"性质。
- 不要在文档中堆砌代码片段超过 10 行——节俭引用，详细看代码。
- 不要把 `REFACTOR_TODO.md` 当作权威——它已经让位。
- 不要新开其他审计文档——所有审计信息都汇总到 `项目代码审计.md`。
